## README

Environment variable that must be set :  

ENV['DROPBOX_API_KEY'] = your dropbox app key  
ENV['DROPBOX_CALLBACK'] = your web app callback URL after user authenticated dropbox  
ENV['API_BASE_URL'] = Base URL for REST API
